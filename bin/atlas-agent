#!/usr/bin/env bash
# atlas-agent entry point wrapper for direct execution
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
REPO_ROOT="$(cd "$SCRIPT_DIR/.." && pwd)"

export PYTHONPATH="$REPO_ROOT/src:$PYTHONPATH"
exec python3 -m atlas_agent "$@"
